name: Release

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      version:
        description: '版本号 (例如: v1.0.0)'
        required: true
        default: 'v1.0.0'

env:
  XCODE_VERSION: '15.0'

jobs:
  build-and-release:
    name: 构建并发布
    runs-on: macos-latest
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: 设置 Xcode 版本
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: latest-stable
    
    - name: 获取版本号
      id: version
      run: |
        if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
          VERSION="${{ github.event.inputs.version }}"
        else
          VERSION="${GITHUB_REF#refs/tags/}"
        fi
        echo "VERSION=$VERSION" >> $GITHUB_ENV
        echo "version=$VERSION" >> $GITHUB_OUTPUT
        echo "📦 版本: $VERSION"
    
    - name: 显示构建信息
      run: |
        echo "🏗️ MacAfk 发布构建"
        echo "版本: ${{ env.VERSION }}"
        echo "分支: ${{ github.ref }}"
        echo "提交: ${{ github.sha }}"
        xcodebuild -version
    
    - name: 构建所有版本
      run: |
        chmod +x build.sh
        VERSION=${{ env.VERSION }} ./build.sh
    
    - name: 验证构建产物
      run: |
        echo "📦 构建产物："
        ls -lh Dist/
        echo ""
        echo "🔐 校验和："
        cat Dist/checksums.txt
    
    - name: 创建 Release Notes
      id: release_notes
      run: |
        cat > release_notes.md << 'EOF'
        ## MacAfk ${{ env.VERSION }}
        
        ### 📦 下载
        
        选择适合你的版本：
        
        #### Pro 版本（推荐）
        - **通用版本** (Apple Silicon + Intel): `MacAfk-Pro-Universal-${{ env.VERSION }}.dmg`
        - **Apple Silicon 专用**: `MacAfk-Pro-arm64-${{ env.VERSION }}.dmg`
        - **Intel 专用**: `MacAfk-Pro-x86_64-${{ env.VERSION }}.dmg`
        
        **特性**：
        - ✅ 真实硬件亮度控制
        - ✅ 更好的省电效果
        - ✅ 独立发布
        
        #### Lite 版本（App Store）
        - **通用版本** (Apple Silicon + Intel): `MacAfk-Lite-Universal-${{ env.VERSION }}.dmg`
        - **Apple Silicon 专用**: `MacAfk-Lite-arm64-${{ env.VERSION }}.dmg`
        - **Intel 专用**: `MacAfk-Lite-x86_64-${{ env.VERSION }}.dmg`
        
        **特性**：
        - ✅ App Store 兼容
        - ✅ 软件调光（Gamma）
        - ✅ 沙盒安全
        
        ### 🔧 安装说明
        
        1. 下载适合你的 DMG 文件
        2. 打开 DMG，将应用拖到"应用程序"文件夹
        3. 首次运行时，在「系统设置 > 隐私与安全性 > 辅助功能」中授予权限
        
        ### ✨ 新功能
        
        - 多语言支持（中文/英文）
        - 抖动间隔设置持久化
        - 优化的用户界面
        - 性能改进
        
        ### 🔐 校验和
        
        下载后请验证文件完整性：
        
        ```bash
        shasum -a 256 MacAfk-*.dmg
        ```
        
        详细校验和请参见 `checksums.txt`
        
        ### 📝 完整更新日志
        
        请查看 [CHANGELOG.md](CHANGELOG.md) 了解详细更改。
        
        ---
        
        **系统要求**：macOS 13.0 或更高版本
        EOF
        
        echo "release_notes<<EOF" >> $GITHUB_OUTPUT
        cat release_notes.md >> $GITHUB_OUTPUT
        echo "EOF" >> $GITHUB_OUTPUT
    
    - name: 创建 GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ env.VERSION }}
        name: MacAfk ${{ env.VERSION }}
        body_path: release_notes.md
        draft: false
        prerelease: false
        files: |
          Dist/*.dmg
          Dist/checksums.txt
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    
    - name: 上传构建产物到 Artifacts
      uses: actions/upload-artifact@v3
      with:
        name: MacAfk-Release-${{ env.VERSION }}
        path: |
          Dist/*.dmg
          Dist/checksums.txt
        retention-days: 90
    
    - name: 通知构建完成
      if: success()
      run: |
        echo "🎉 Release 发布成功！"
        echo "版本: ${{ env.VERSION }}"
        echo "查看: https://github.com/${{ github.repository }}/releases/tag/${{ env.VERSION }}"
    
    - name: 通知构建失败
      if: failure()
      run: |
        echo "❌ Release 发布失败"
        echo "请检查构建日志"

